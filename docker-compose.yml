# docker-Specify the compose version
version: "3"

services:
  app:
    # build: .
    image: pudding/github-action-app:puppeteer-python-14-20230530-0616
    tty: true
    volumes: 
      - ./app:/app/
      - ./output:/output/
      - ./cache:/cache/
      - ./config.js:/config.js