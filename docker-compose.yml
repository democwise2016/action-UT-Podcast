# docker-Specify the compose version
version: "3"

services:
  app:
    # build: .
    image: pudding/github-action-app:puppeteer-python-14
    tty: true
    volumes: 
      - ./app:/app/
      - ./output:/output/
      - ./cache:/cache/
      - ./config.js:/config.js